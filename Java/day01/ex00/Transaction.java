import java.util.UUID;
public class Transaction
{
    private:
        UUID Identifier;
        Recipient (User type)
        Sender (User type)
        Transfer category (debits, credits)
        Transfer amount
}